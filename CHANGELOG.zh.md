# 更新日志

## 0.3.0 - 2026-08-07

### 破坏性变更

- 架构迁移：bot 不再为每条消息 spawn 一个 `mimo run` 进程，改为管理常驻的 `mimo serve` 子进程，通过 HTTP + SSE 通信。会话持久化在服务端（SQLite），bot 或 serve 重启后仍可继续。
- 移除仅 CLI 可用的命令：`/models`、`/stats`、`/export`、`/providers`（新架构下无对应服务端 API）。
- `MIMO_API_URL` 语义变化：现在表示连接一个已运行的 `mimo serve`，而非将 `mimo run` attach 到远程服务。

### 新功能

- 常驻 `mimo serve`（托管子进程）：自动拉起、健康检查、崩溃自动重启、优雅退出；`MIMO_SERVE_PORT` 配置端口（默认 4096）。
- 交互式权限审批：权限请求以 Telegram 内联按钮呈现（✅ 允许一次 / ⚡ 总是允许 / ❌ 拒绝），不再静默拒绝或全量放行；CSPRNG token + TTL 清理。
- `/cancel` 通过服务端 API 中止任务，并处理竞态窗口（会话创建期间的取消也会生效）。
- `/workdir` 切换目录时重启托管 serve；会话在切换后仍可继续。
- `/use` 动态列出服务端 agent；`/status` 显示服务器地址、版本与权限模式。
- 模型/供应商错误（如模型不支持、配额耗尽）会展示给用户，而不是返回空回复。
- 失效会话恢复同时兼容 404 响应与 serve < v0.1.10 的 200+空响应。
- `/new`、`/delete` 对已删除（失效）会话容错。

### 修复

- prompt 响应体 parts 作为权威内容来源，SSE 累积文本作为兜底——无 SSE 订阅时也能返回内容。
- 修复 `/workdir` 切换失败时无错误反馈（重复应答回调查询吞掉了提示）。
- 权限 token 改用 CSPRNG；权限字符串 HTML 转义；回复失败时保留按钮以便重试。

### 测试

- 198 个单元测试，覆盖 fake HTTP + SSE 服务（会话 CRUD、prompt 参数、SSE 事件转换、失效会话恢复、超时、取消竞态、权限路由）。
- 13/13 集成测试通过，基于真实 `mimo serve`，含一次真实模型往返。
